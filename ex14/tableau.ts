function tableau ():number[]{

    // var tab = [0, 0, 0, 0, 0,0];
    var tab : number[] = []
    for (let i = 0; i <=6; i++)
    {
        tab[i]=0
    }

    
    
return(tab)
}

console.log(tableau());
